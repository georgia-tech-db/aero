# This file was generated using the `serve build` command on Ray v2.10.0.

proxy_location: EveryNode

http_options:

  host: 0.0.0.0

  port: 8001

grpc_options:

  port: 9001

  grpc_servicer_functions: []

logging_config:

  encoding: TEXT

  log_level: INFO

  logs_dir: null

  enable_access_log: true

applications:

- name: app1

  route_prefix: /

  import_path: ray_serve_hydro:eva_app

  runtime_env: {}

  deployments:

  - name: Hydro
    num_replicas: 1
    ray_actor_options:
      num_cpus: 0.2
      num_gpus: 0.0
